{% block body %}

    {{ form_start(form) }}
    <div class="modal-body">
        <div class="container-fluid">
            <div class="form-row">
                <div class="col">
                    <p>Are you sure you want to change the pic-sure status? Common cases for this would be:</p>
                    <div class="list-group cases">
                        <a class="list-group-item list-group-item-action">pic-sure was correctly done on a different policy and this policy is a continuation of the older policy</a>
                        <a class="list-group-item list-group-item-action">We rejected pic-sure, either incorrectly, or the user had a screen protector</a>
                        <a class="list-group-item list-group-item-action">There was a sucessful claim with a voucher and pic-sure needs to be re-done</a>
                        <a class="list-group-item list-group-item-action">PH got replacement device under manufacture warranty</a>
                        <a class="list-group-item list-group-item-action bg-danger"><span class="text-danger">Upgraded device - within premium price</span></a>
                    </div>
                </div>
            </div>

            <div class="form-row">
                <div class="checkbox">
                    <label>
                        <input type="checkbox" required> I've read the list of common reasons above
                    </label>
                </div>

                <div class="checkbox">
                    <label>
                        <input type="checkbox" required> I understand I should not update pic-sure based on a standard claim as this will be done automatically
                    </label>
                </div>

                <div class="checkbox">
                    <label>
                        <input type="checkbox" required> If it is not a common reason, I have verified the exception is allowed
                    </label>
                </div>
            </div>

            <hr>
            <div class="form-row">
                <label>Current Status</label>
                {{ policy.picSureStatus }}
            </div>

            <div class="form-row">
                <label>Status</label>
                {{ form_widget(form.picSureStatus, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-row">
                <label>Justification</label>
                {{ form_widget(form.note, {'attr': {'class': 'form_note form-control'}}) }}
            </div>
        </div>
    </div>

    <div class="modal-footer">
        {{ form_widget(form.update, {'attr': {'class': 'btn btn-warning', 'disabled': 'true'}}) }}
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
    </div>
    {{ form_end(form) }}

{% endblock %}
