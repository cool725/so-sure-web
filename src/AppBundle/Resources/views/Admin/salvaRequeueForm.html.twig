{% block body %}

    {{ form_start(form) }}
    <div class="modal-body">
        <div class="container-fluid">
            <div class="form-group">
                <div class="col">
                    <p>Are you sure you want to requeue this policy?</p>
                </div>
            </div>

            <div class="form-group">
                <label>Salva Policy Status</label>
                <p>{{ policy.salvaStatus }}</p>
            </div>

            <div class="form-group">
                <label>Reason</label>
                {{ form_widget(form.reason, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
    </div>

    <div class="modal-footer">
        {{ form_widget(form.requeue, { 'attr': {'class': 'btn btn-danger'} }) }}
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
    </div>
    {{ form_end(form) }}

{% endblock %}
