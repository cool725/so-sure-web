<html>
    <head>
        <meta http-equiv="content-type" content="text/html;charset=utf-8" />
    </head>
    <body>

        <h1>so-sure Accounts for {{ month }} / {{ year }}</h1>

<table class="table table-striped">
    <tr class="info">
        <td>Total for month</td>
        <td>£{{ paymentTotals.total|number_format(2, '.', ',') }}</td>
    </tr>
    <tr>
        <td>Paid in</td>
        <td>£{{ paymentTotals.received|number_format(2, '.', ',') }}</td>
    </tr>
    <tr>
        <td>Refunded</td>
        <td>£{{ paymentTotals.refunded|number_format(2, '.', ',') }}</td>
    </tr>
    <tr>
        <td>Total Commissions (so-sure + Afl)</td>
        <td>£{{ paymentTotals.totalCommission|number_format(2, '.', ',') }} ( {{ paymentTotals.totalCommissionPercent |number_format(1, '.', ',')}}% )</td>
    </tr>
    <tr class="success">
        <td>so-sure Commission</td>
        <td>£{{ paymentTotals.coverholderCommission|number_format(2, '.', ',') }} ( {{ paymentTotals.coverholderCommissionPercent |number_format(1, '.', ',')}}% )</td>
    </tr>
    <tr class="danger">
        <td>Salva</td>
        <td>£{{ paymentTotals.totalUnderwriter|number_format(2, '.', ',') }} ( {{ paymentTotals.totalUnderwriterPercent |number_format(1, '.', ',')}}% )</td>
    </tr>
    <tr class="danger">
        <td>Afl Commission</td>
        <td>£{{ paymentTotals.brokerCommission|number_format(2, '.', ',') }} ( {{ paymentTotals.brokerCommissionPercent |number_format(1, '.', ',')}}% )</td>
    </tr>
    <tr>
        <td>Number of Payments Received</td>
        <td>{{ paymentTotals.numPayments }}</td>
    </tr>
    <tr>
        <td>Average Payment Amount</td>
        <td>{% if paymentTotals.avgPayment %}£{{ paymentTotals.avgPayment|number_format(2, '.', ',')  }}{% else %}n/a{% endif %}</td>
    </tr>
    <tr>
        <td>Number of Active Policies (as of end {{ month }} / {{ year }})</td>
        <td>{{ activePolicies }}</td>
    </tr>
</table>
    </body>
</html>
