<VirtualHost *:80>
        ServerAdmin tech@so-sure.com
     	ServerName localhost
        DocumentRoot {{ sosure_path }}
        <Location />
  	       RewriteEngine On
     	   RewriteCond %{REQUEST_FILENAME} !-f
	       RewriteRule ^(.*)$ app.php [QSA,L]
           Require all granted
        </Location>
</VirtualHost>
