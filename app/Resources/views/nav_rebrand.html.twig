<nav class="navbar fixed-top justify-content-between flex-nowrap flex-row pl-lg-5">
    <a href="/" class="navbar-brand show-in-nav"><div class="navbar-brand__logo"></div></a>
    <ul class="nav navbar-nav flex-row align-items-center">
        <li class="nav-item d-none d-lg-block pr-lg-5">
            <a href="#" class="nav-link">About</a>
        </li>
        <li class="nav-item d-none d-lg-block pr-lg-5">
            <a href="#" class="nav-link">Company phones</a>
        </li>
        <li class="nav-item d-none d-lg-block pr-lg-5">
            <a href="#" class="nav-link">FAQs</a>
        </li>
        <li class="nav-item d-none d-lg-block pr-lg-5">
            <a href="#" class="nav-link">Blog</a>
        </li>
        <li class="nav-item d-none d-lg-block pr-lg-5">
            <a href="#" class="nav-link">Contact</a>
        </li>
        {% if app.user is not null and app.user %}
            <li class="nav-item d-none d-lg-block pr-lg-5">
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('admin_home') }}" class="nav-link text-primary">Admin <i class="fas fa-users-cog fa-sm"></i></a>
                {% elseif is_granted('ROLE_EMPLOYEE') %}
                    <a href="{{ path('admin_home') }}" class="nav-link text-primary">Admin <i class="fas fa-user-tie fa-sm"></i></a>
                {% elseif is_granted('ROLE_CLAIMS') %}
                    <a href="{{ path('claims_home') }}" class="nav-link text-primary">Claims <i class="fas fa-user-shield fa-sm"></i></a>
                {% else %}
                    <a href="{{ path('user_home') }}" class="nav-link text-primary">Your account <i class="fas fa-user-circle fa-sm"></i></a>
                {% endif %}
            </li>
        {% else %}
            <li class="nav-item pr-lg-5">
                <a id="quote-btn" href="#" class="btn btn-primary btn-sm btn-gradient show-in-nav">Get a Quote</a>
            </li>
        {% endif %}
        <li class="nav-item d-none d-lg-block pr-lg-5">
            {% if app.user is not null and app.user %}
                {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                    <a href="{{ path('homepage', {'_impersonate': '_exit'}) }}" class="nav-link text-primary">Exit <u>{{ app.user.email }}</u></a>
                {% else %}
                    <a href="{{ path('fos_user_security_logout') }}" class="nav-link text-primary">Log out</i></a>
                {% endif %}
            {% else %}
                <a href="{{ path('fos_user_security_login') }}" class="nav-link text-primary">Customer log in <i class="fas fa-arrow-right fa-sm ml-2"></i></a>
            {% endif %}
        </li>
        <li class="nav-item d-lg-none">
            <button id="nav_toggle" class="hamburger hamburger--spring-r hamburger--collapse" type="button">
                <span class="hamburger-box">
                    <span class="hamburger-inner"></span>
                </span>
            </button>
        </li>
    </ul>
    <div id="menu" class="menu d-flex flex-column d-lg-none">
        <ul class="list-unstyled align-self-stretch px-5 menu__links">
            <li class="mb-4">
                <a href="{{ path('about_home') }}" class="h4">About</a>
            </li>
            <li class="my-4">
                <a href="{{ path('claim') }}" class="h4">Making a claim</a>
            </li>
            <li class="my-4">
                <a href="{{ path('faq') }}" class="h4">FAQs</a>
            </li>
            <li class="my-4">
                <a href="{{ path('blog') }}" class="h4">Blog</a>
            </li>
            <li class="my-4">
                <a href="{{ path('about_how_to_contact_so_sure') }}" class="h4">Contact</a>
            </li>
        </ul>
        <div class="p-5 align-self-stretch menu__buttons">
            <a href="#" class="btn btn-primary btn-shadow btn-gradient btn-block mb-3">Get insured</a>
            <a href="#" class="btn btn-outline-white btn-block"><i class="far fa-play-circle fa-lg"></i> Watch our Video</a>
        </div>
    </div>
</nav>
