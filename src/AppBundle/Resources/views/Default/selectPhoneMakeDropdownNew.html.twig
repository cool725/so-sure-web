{# {% set uid = '' %}
{% set formId = '' %}
{% set buttonText = 'Quote me' %}
{% set formClass = '' %} #}

{# {% if type is defined and type == 'purchase-select' %}
    {% set buttonText = 'Select Phone' %}
{% elseif type is defined and type == 'purchase-change' %}
    {% set buttonText = 'Select Phone' %}
{% elseif type is defined and type == 'homepage-sticky' %}
    {% set uid = '-1' %}
    {% set formClass = 'dropdown-phone-form--homepage' %}
    {% set formId = 'dropdown-phone-form' ~ uid %}
{% elseif type is defined and type == 'footer' %}
    {% set uid = '-2' %}
    {% set formId = 'dropdown-phone-form' ~ uid %}
{% else %}
    {% set buttonText = 'Quote me' %}
{% endif %} #}
{#
<div id="select-phone-data"
     class="dropdown-phone-form {{ formClass }}"
     data-phones='{{ phones|json_encode|raw }}'>
    {{ form_start(form_phone, { 'attr': { 'id': formId, 'autocomplete': 'off' }}) }}
        <div class="form-group">
            {{ form_widget(form_phone.make, {
              'attr': {
                'class': 'select-phone-option select-phone-make form-control animated flipInX'
            }}) }}
            {{ form_widget(form_phone.model, {
              'attr': {
                'class': 'select-phone-option select-phone-model form-control animated flipInX select-phone-option-focus',
                'style': 'display:none;'
            }}) }}
            {{ form_widget(form_phone.memory, {
                'attr': {
                    'class': 'select-phone-option select-phone-memory form-control animated flipInX select-phone-option-focus',
                    'style': 'display:none;'
            }}) }}
            {{ form_widget(form_phone.next, {
              'label': buttonText,
              'attr': {
                'class': 'btn btn-green btn-block btn-pad select-phone-btn animated bounceIn',
                'style': 'display:none;'
            }}) }}
        </div>
    {{ form_end(form_phone) }}
</div> #}


<div id="dropdown-search"
     class="dropdown-phone-form--homepage"
     data-phones='{{ phones|json_encode|raw }}'>
    {{ form_start(form_phone, { 'attr': { 'autocomplete': 'off', 'class': '' }}) }}
        <div class="row">
            <div class="col-sm-4">
                {{ form_widget(form_phone.make, {
                  'attr': {
                    'class': 'form-control dropdown-phone-form--homepage_make'
                }}) }}
            </div>
            <div class="col-sm-4">
                {{ form_widget(form_phone.model, {
                    'attr': {
                        'class': 'form-control dropdown-phone-form--homepage_model',
                        'disabled': 'disabled'
                }}) }}
            </div>
            <div class="col-sm-4">
                {{ form_widget(form_phone.memory, {
                    'attr': {
                        'class': 'form-control dropdown-phone-form--homepage_memory',
                        'disabled': 'disabled'
                }}) }}
            </div>
        </div>
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                <div class="form-group">
                    {{ form_widget(form_phone.next, {
                      'label': 'Get a quote',
                      'attr': {
                        'class': 'btn btn-green btn-block btn-pad'
                    }}) }}
                </div>
            </div>
        </div>
    {{ form_end(form_phone) }}
</div>
