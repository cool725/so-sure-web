version: 0.2
os: linux
run-as: root
phases:
  install:
    commands:
      - cd $CODEBUILD_SRC_DIR; docker build -t test .
  build:
    commands:
      - docker run --privileged --ulimit nofile=100000:100000 -t test
