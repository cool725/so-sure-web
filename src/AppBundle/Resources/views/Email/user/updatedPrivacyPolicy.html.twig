{% set logo_image = 'happy' %}
{% extends 'AppBundle:Email:base.html.twig' %}
{% import "AppBundle:Email:macros.html.twig" as macros %}
{% block title %}Updated privacy policy.{% endblock %}
{% block body %}
	{{ macros.start_table() }}
	<div style="font-size: 14px; text-align: left;">
	   <!--?xml version="1.0" encoding="UTF-8" standalone="no"?-->
	   <div style="font-size: 14px; text-align: center;"><span style="font-family:arial,helvetica neue,helvetica,sans-serif">
			Hi {% if user.name|length > 1 %}{{ user.name }}{% else %}{{ user.email }}{% endif %},
			<br><br>
			One of the biggest changes to UK data privacy law comes into effect on 25 May 2018. The General Data Protection Regulation, also known as GDPR, means that you'll have more control over how your data is used.
			<br><br>
			GDPR ensures that organisations protect your personal data better. To reflect these changes and new obligations, we've updated our <a href="{{ url('privacy') }}">privacy privacy</a> which now tells you what we do with your personal data, how it's used and your rights as an individual under the new law.
			<br><br>
			Should you with to opt-out of receiving information from so-sure you can do so <a href="{{ url('optout_hash', {'hash': hash}) }}">here</a>.
	   </div>
	</div>
	{{ macros.end_table() }}
    {{ macros.email_button_table(url('optout_hash', {'hash': hash}), "Update Preferences", "Update Preferences")}}
{% endblock %}
