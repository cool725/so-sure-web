{% extends 'admin_claims_base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {% javascripts
        '@AppBundle/Resources/public/js/AdminEmployee/kpi.js'
    %}
    <script src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}

{% block body %}
<div class="container" style="margin-bottom: 50px">

    <div class="row">
        <div class="col-md-6">
            <h1>so-sure KPI</h1>
        </div>
        <form id='kpi-form' method="POST">
            <div class="col-md-2">
                <div class="form-group" style="padding-top: 15px;">
                    <div class='input-group date' id='kpi'>
                        <input name="kpi" type='text' class="form-control" value="{{ now|date('d-m-Y') }}" />
                        <span class="input-group-addon">
                            <span class="fa fa-calendar"></span>
                        </span>
                    </div>
                </div>
            </div>
            <div class="col-md-2">
                <div class="form-group" style="padding-top: 15px;">
                    <button class="btn btn-primary">Adjust date</button>
                <a href="{{ previous_page }}"><i class="fa fa-arrow-left"></i></a>
                <a href="{{ next_page }}"><i class="fa fa-arrow-right"></i></a>
                </div>
            </div>
        </form>
    </div>
    <div class="row">
        <div class="col-md-11">
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                    <tr>
                    <th></th>
                    {% for week in weeks %}
                        <td>Ending <b>{{ week.end_date_disp|date('d M Y') }}</b><br>
                            <small>{{ week.start_date|date('d M Y') }} - {{ week.end_date_disp|date('d M Y') }}</small><br>
                            <small>{{ week.start_date|date('H:i') }} - {{ week.end_date_disp|date('H:i') }}</small>
                        </td>
                    {% endfor %}
                    </tr>
                    </thead>
                    {# dump(weeks) #}
                    <tbody>
                        <tr>
                            <td>Run Rate Premium [KPI: 12]</td>
                            {% for week in weeks %}
                                <td>£{{ week.total.data['totalRunRate'] |number_format(2, '.', ',') }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Active Policies [KPI: 14]</td>
                            {% for week in weeks %}
                                {# as upgrades will be included in new and taken off ending is the same as if using adjUpgrade on both #}
                                <td>{{ (week.total.data['newPolicies'] - week.total.data['endingEndingPolicies']) }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Percentage of FNOL within 30days policy inception [KPI: 68]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['fnol30Claims']|number_format(1, '.', ',') }}%</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Overall Approved Claim Rate- rolling 12mo [KPI: 69] <i class="fa fa-info" title="12 month FNOL that were approved / current active policies"></i></td>
                            {% for week in weeks %}
                                <td>{%  if (week.total.data['newPolicies'] - week.total.data['endingEndingPolicies']) != 0  %}
                                        {{ (100 * week.period.data['rolling-yearly-claims-totals'] / (week.total.data['newPolicies'] - week.total.data['endingEndingPolicies']))|number_format(1, '.', ',') }}%
                                    {% else %}
                                        N/A
                                    {% endif %}
                                </td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Average cost of claims in month [KPI: 70]</td>
                            {% for week in weeks %}
                                <td></td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># of User aquired [KPI: 75]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['newUsers'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># of New Policyholders [KPI: 77]</td>
                            {% for week in weeks %}
                                <td></td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>New Policies [KPI: 81] (excludes upgrades & renewals)</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['newPoliciesAdjUpgradeRenewals'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Policy Upgrades [KPI: 83]
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['endingUpgradePolicies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Policy Expired [KPI: 86]
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['endingExpiredPolicies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Policy Renewed [KPI: 87]
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['endingPoliciesRenewed'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Policy unable to renew (so-sure declined) [KPI: 88]
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['endingPoliciesRenewedDeclined'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total Cancellations - User Requested [KPI: 98]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['endingUserRequestedPolicies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total Cancellations - Unpaid [KPI: 99]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['endingUnpaidPolicies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total Cooloffs [KPI: 100]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['endingCooloffPolicies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total Cancellations - Other [KPI: 101]</td>
                            {% for week in weeks %}
                                <td>{{ (week.period.data['endingActualFraudPolicies'] + week.period.data['endingSuspectedFraudPolicies'] + week.period.data['endingBadRiskPolicies'] + week.period.data['endingDispossessionPolicies'] + week.period.data['endingWreckagePolicies']  )}}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Undesired Cancellations - User Requested [KPI: 104]</td>
                            {% for week in weeks %}
                                <td>{{ (week.period.data['endingUserRequestedPolicies'] - week.period.data['desiredEndingUserRequestedPolicies']) }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Undesired Cancellations - Unpaid [KPI: 105]</td>
                            {% for week in weeks %}
                                <td>{{ (week.period.data['endingUnpaidPolicies'] - week.period.data['desiredEndingUnpaidPolicies']) }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Undesired Cooloffs [KPI: 106]</td>
                            {% for week in weeks %}
                                <td>{{ (week.period.data['endingCooloffPolicies'] - week.period.data['desiredEndingCooloffPolicies']) }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Undesired Cancellations - Other [KPI: 107]</td>
                            {% for week in weeks %}
                                <td>{{ ( week.period.data['endingActualFraudPolicies'] + week.period.data['endingSuspectedFraudPolicies'] + week.period.data['endingBadRiskPolicies'] + week.period.data['endingDispossessionPolicies'] + week.period.data['endingWreckagePolicies']  - week.period.data['desiredEndingActualFraudPolicies'] - week.period.data['desiredEndingSuspectedFraudPolicies'] - week.period.data['desiredEndingBadRiskPolicies'] - week.period.data['desiredEndingDispossessionPolicies'] - week.period.data['desiredEndingWreckagePolicies'] ) }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># of Total Policies that are Cancelled w/approved Claim (Payment owed) [KPI: 111]</td>
                            {% for week in weeks %}
                                <td>{{ week['kpi-cancelled-payment-owed'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># of Policies Paid that were Cancelled w/approved Claim (Payment owed) [KPI: 112]</td>
                            {% for week in weeks %}
                                <td>{{ week['kpi-cancelled-payment-paid'] }}</td>
                            {% endfor %}
                        </tr>

                        <tr>
                            <td>Activation Policies [KPI??] (excludes upgrades & renewals)<br>
                            <span title="Initial data population was run on 20/4/18 and populated based on policies that were still activated as of that date">
                                <i class="fa fa-warning"></i>
                                before 31/3/18 data is suspect!
                            </span>
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['activatedPoliciesActivatedAdjUpgradeRenewals'] }} / {{ week.period.data['activatedPoliciesTotalAdjUpgradeRenewals'] }}
                                    ({{ week.period.data['activatedPoliciesActivatedAdjUpgradeRenewalsPercent']|number_format(1, '.', ',') }}%)
                                    <br><small>{{ week.period.startActivation|date('d M Y ') }} - {{ week.period.endActivationDisp|date('d M Y') }}</small>
                                    <br><small>{{ week.period.startActivation|date('H:i') }} - {{ week.period.endActivationDisp|date('H:i') }}</small>
                                </td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Hard Activation Policies [KPI??] (excludes upgrades & renewals)
                                <span title="Initial data population was run on 20/4/18 and populated based on policies that were still activated as of that date">
                                <i class="fa fa-warning"></i>
                                before 15/1/18 data is suspect!
                            </span>
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['hardActivatedPoliciesAdjUpgradeRenewals'] }} / {{ week.period.data['hardActivatedPoliciesTotalAdjUpgradeRenewals'] }}
                                    ({{ week.period.data['hardActivatedPoliciesAdjUpgradeRenewalsPercent']|number_format(1, '.', ',') }}%)
                                    <br><small>{{ week.period.startHardActivation|date('d M Y') }} - {{ week.period.endHardActivationDisp|date('d M Y') }}</small>
                                    <br><small>{{ week.period.startHardActivation|date('H:i') }} - {{ week.period.endHardActivationDisp|date('H:i') }}</small>
                                </td>
                            {% endfor %}
                        </tr>

                        <tr>
                            <td>Number of active Policyholders [KPI: 115]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['totalActivePolicyHolders'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>% of active policyholders with >1 policy [KPI: 116]</td>
                            {% for week in weeks %}
                                <td></td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>FNOL Claims [KPI: 119]
                                    <span class="badge" title="Days delayed - requires input by davies so may be missed">-3 (approx)</span>
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period['claims']['total'] }}
                                    <i class="fa fa-clock-o" title="Value is delayed"></i>
                                </td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Approved Claims [KPI: 121]
                                    <span class="badge" title="Days delayed - requires input by davies so may be missed">-3 (approx)</span>
                            </td>
                            {% for week in weeks %}
                                <td>{{ week.period['approvedClaims']['total'] }}
                                    <i class="fa fa-clock-o" title="Value is delayed"></i>
                                </td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Approved Claim Freq (avg weekly)</td>
                            {% for week in weeks %}
                                <td>{{ (100 * week['freq-claims']) | number_format(1, '.', ',') }}%</td>
                            {% endfor %}
                        </tr>
                        {#}
                        <tr>
                            <td># New Connections [KPI: 97]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['newActiveConnections'] - week.period.data['newEndedConnections'] }} ( Active: {{ week.period.data['newActiveConnections'] }} - Ending: {{ week.period.data['newEndedConnections'] }} )</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># Total Connections [KPI: 99]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['totalActiveConnections'] }}</td>
                            {% endfor %}
                        </tr>
                        #}
                        <tr>
                            <td># Total Unique Policies w/Connections [KPI: 143]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['newTotalPoliciesWithConnections'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total # pic-sure unstarted policies (pic-sure policy, but no upload) [KPI: 144]</td>
                            {% for week in weeks %}
                                <td>{{ week['kpi-picsure-unstarted-policies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total # pic-sure invalid policies, (pic-sure started, but not completed) [KPI: 145]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-invalid-policies'] is defined %}{{ week['kpi-picsure-invalid-policies'] }}{% else %}-{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total # pic-sure approved policies [KPI: 146]</td>
                            {% for week in weeks %}
                                <td>{{ week['kpi-picsure-approved-policies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total # pic-sure pre-approved (renewals) policies [KPI: 147]</td>
                            {% for week in weeks %}
                                <td>{{ week['kpi-picsure-preapproved-policies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total # pic-sure claims approved (rejected/invalid/unstarted -> claims -> claims-approved) policies [KPI: ??]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-claims-approved-policies'] is defined %}{{ week['kpi-picsure-claims-approved-policies'] }}{% else %}??{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total # pic-sure rejected policies [KPI: 148]</td>
                            {% for week in weeks %}
                                <td>{{ week['kpi-picsure-rejected-policies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Active # pic-sure unstarted policies (pic-sure policy, but no upload) [KPI: ??]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-active-unstarted-policies'] is defined %}{{ week['kpi-picsure-active-unstarted-policies'] }}{% else %}??{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Active # pic-sure invalid policies, (pic-sure started, but not completed) [KPI: ??]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-active-invalid-policies'] is defined %}{{ week['kpi-picsure-active-invalid-policies'] }}{% else %}-{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Active # pic-sure approved policies [KPI: ??]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-active-approved-policies'] is defined %}{{ week['kpi-picsure-active-approved-policies'] }}{% else %}??{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Active # pic-sure pre-approved (renewals) policies [KPI: ??]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-active-preapproved-policies'] is defined %}{{ week['kpi-picsure-active-preapproved-policies'] }}{% else %}??{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Active # pic-sure claims approved (rejected/invalid/unstarted -> claims -> claims-approved) policies [KPI: ??]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-active-claims-approved-policies'] is defined %}{{ week['kpi-picsure-active-claims-approved-policies'] }}{% else %}??{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Active # pic-sure rejected policies [KPI: ??]</td>
                            {% for week in weeks %}
                                <td>{% if week['kpi-picsure-active-rejected-policies'] is defined %}{{ week['kpi-picsure-active-rejected-policies'] }}{% else %}??{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Claim Attribution [KPI: REMOVED]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['claimAttributionText'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Rolling 12 month Claim Attribution [KPI: 187-196]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['rolling12MonthClaimAttributionText'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>New Policies via Invitation [KPI: 213]</td>
                            {% for week in weeks %}
                                <td>{{ (week.period.data['newInvitationPolicies'] + week.period.data['newSCodePolicies']) }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>App New Policy Holders [KPI: 218]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-purchase-policy-app-attrib'] is defined %}{{ week['mixpanel-purchase-policy-app-attrib'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Total Site Visitors [KPI: 409]</td>
                            {% for week in weeks %}
                                <td>{{ week['mixpanel-total-site-visitors'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Quote Page UK [KPI: 410]</td>
                            {% for week in weeks %}
                                <td>{{ week['mixpanel-quotes-uk'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>CPC Quote Page [KPI: 411]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-cpc-quotes-uk'] is defined %}{{ week['mixpanel-cpc-quotes-uk'] }}{% elseif week['mixpanel-landing-uk'] is defined %}{{ week['mixpanel-landing-uk'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>CPC Manufacturer Page [KPI: 412]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-cpc-manufacturer-uk'] is defined %}{{ week['mixpanel-cpc-manufacturer-uk'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>CPC Competitors Page [KPI: 413]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-cpc-competitors-uk'] is defined %}{{ week['mixpanel-cpc-competitors-uk'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Click Buy Now [KPI: 414]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-click-buy-now'] is defined %}{{ week['mixpanel-click-buy-now'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Receive Personal Details [KPI: 415]</td>
                            {% for week in weeks %}
                                <td>{{ week['mixpanel-receive-personal-details'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># Sent Invitations - Email & SMS [KPI: 467]</td>
                            {% for week in weeks %}
                                <td>{{ week.period.data['newInvitationPolicies'] }}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># Sent Invitations - SCode [KPI: 468]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-invite-someone'] is defined %}{{ week['mixpanel-invite-someone'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># Viewed Invitations - Email [KPI: 471]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-view-invitation-email'] is defined %}{{ week['mixpanel-view-invitation-email'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># Viewed Invitations - SCode [KPI: 472]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-view-invitation-scode'] is defined %}{{ week['mixpanel-view-invitation-scode'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td># Viewed Invitations - SMS [KPI: 473]</td>
                            {% for week in weeks %}
                                <td>{% if week['mixpanel-invite-someone'] is defined %}{{ week['mixpanel-invite-someone'] }}{% else %}unknown - see mixpanel{% endif %}</td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Apple App Installs [KPI: 476]
                                    <span class="badge" title="Days delayed">-4</span>
                            </td>
                            {% for week in weeks %}
                                <td>
                                    {{ week['install-apple'] }}
                                    <i class="fa fa-clock-o" title="Value is delayed"></i>
                                </td>
                            {% endfor %}
                        </tr>
                        <tr>
                            <td>Google App Installs [KPI: 477]
                                    <span class="badge" title="Days delayed">-4</span>
                            </td>
                            {% for week in weeks %}
                                <td>
                                    {{ week['install-google'] }}
                                    <i class="fa fa-clock-o" title="Value is delayed"></i>
                                </td>
                            {% endfor %}
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}
