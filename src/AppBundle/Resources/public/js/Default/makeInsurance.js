// Make Insurance JS
$(function(){

    // Init carousel
    $('.owl-carousel').owlCarousel({
        margin: 30,
        stagePadding: 100,
        // autoWidth: true,
        items: 1,
    });

});
